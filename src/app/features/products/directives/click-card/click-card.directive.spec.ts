import { ClickCardDirective } from './click-card.directive';

describe('ClickCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickCardDirective();
    expect(directive).toBeTruthy();
  });
});
