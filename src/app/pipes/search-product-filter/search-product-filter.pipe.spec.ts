import { SearchProductFilterPipe } from './search-product-filter.pipe';

describe('SearchProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
