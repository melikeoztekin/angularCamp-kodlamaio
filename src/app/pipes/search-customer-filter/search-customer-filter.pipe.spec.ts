import { SearchCustomerFilterPipe } from './search-customer-filter.pipe';

describe('SearchCustomerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCustomerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
