import { SearchCategoryFilterPipe } from './search-category-filter.pipe';

describe('SearchCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
